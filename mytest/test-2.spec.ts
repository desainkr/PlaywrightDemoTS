import { test, expect,devices} from '@playwright/test';

test('test', async ({ page }) => {
  // await page.goto('https://conduit.bondaracademy.com/');
  // await page.getByRole('link', { name: 'Sign in' }).click();
  // await page.getByRole('textbox', { name: 'Email' }).fill('testgen@test.com');
  // await page.getByRole('textbox', { name: 'Password' }).fill('test12345678');
  // await page.getByRole('button', { name: 'Sign in' }).click();
  // await page.getByRole('link', { name: '  New Article' }).click();
  // await page.getByRole('textbox', { name: 'Article Title' }).click();
  // await page.getByRole('textbox', { name: 'Article Title' }).fill('Test Title1234445551');
  // await page.getByRole('textbox', { name: 'What\'s this article about?' }).fill('Test body12344222122');
  // await page.getByRole('textbox', { name: 'Write your article (in' }).fill('Testing purpose2221444');
  // await page.getByRole('textbox', { name: 'Enter tags' }).fill('Testing');
  // await page.getByRole('button', { name: 'Publish Article' }).click();
  // await expect(page.getByRole('navigation')).toContainText('conduit');
  // await page.getByRole('button', { name: 'Delete Article' }).first().click();
 
    console.log(Object.keys(devices));

});

test

if (browsername == 'firefox'){

  test.skip();
}